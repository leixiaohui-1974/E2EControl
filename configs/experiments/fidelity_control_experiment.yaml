experiment_name: fidelity_control_effectiveness
seed: 42
scenario_count: 80
scenario_pool_factor: 4

strict_revalidation:
  enabled: true
  modules:
    - physics
    - control
  physics_backend: auto
  controller_backend: base_mpc

backend_comparison:
  enabled: true
  modules:
    - physics
    - control
  controller_backend: base_mpc

outputs:
  directory: reports/experiments/fidelity_control_effectiveness
  strict_json: strict_revalidation.json
  backend_json: backend_comparison.json
  summary_json: summary.json
  summary_md: summary.md
